# GenCodeDoc v2.1.0 — Walkthrough 🚀

## Overview
This release completes the **Post-Audit Update** of GenCodeDoc, focusing on cleaning up technical debt, activating dormant features, implementing configuration flexibility, and establishing a robust test suite.

## Key Changes

### 1. Configuration & Presets ⚙️
- **YAML Presets**: Configuration presets (`python`, `nodejs`, `web`, `go`) are now external YAML files in `gencodedoc/config/presets/`, allowing users to customize them without changing source code.
- **Debug Mode**: Added `GENCODEDOC_DEBUG` environment variable support for verbose logging.

### 2. Core Features 🧩
- **Compression**: Enabled `zstandard` compression based on `bucket.compression_enabled` config.
- **Retention Policy**: Implemented `cleanup_orphaned_contents` and autosave expiration logic.
- **CLI Improvements**: Added `cat`, `files`, `export`, and `restore --filter` commands.
- **MCP Tools**: Expanded MCP capabilities with 6 new tools including `export_snapshot` and `restore_files`.

### 3. Cleanup & Optimization 🧹
- **Removed Bloat**: Deleted the 2.4MB `sqlite3` PostScript file that was mistakenly included.
- **Dependencies**: Cleaned up `pyproject.toml` (removed `httpx`, `aiofiles`, etc.).
- **Makefile**: Made compatible with any environment (removed hardcoded paths).

### 4. Testing & Quality 🧪
- **Test Suite**: Created a comprehensive `pytest` suite for core logic, utils, and CLI (`tests/` directory).
- **Coverage**: Achieved ~44% code coverage, verifying critical paths.

## Validation Results

### CLI Tests
- `gencodedoc init` ✅
- `gencodedoc snapshot create` ✅
- `gencodedoc status` ✅
- **Regression Check**: Confirmed that `mcp-server-stdio` works correctly with the new structure.

### Synchronization
- **GitHub Repo**: `io.github.fkom13/gencodedoc` is now fully synced with the local development version.
- **Push Status**: Success (Commit `feat(v2.1.0): Post-Audit Update`).

## Documentation 📚
- **English**: Updated internal `README.md` and synced to `DOCUMENTATION.md` in public repo.
- **French**: Updated public `README.md` with V2.1 features.

---
*Generated by Gemini Oracle Agent — 2026-02-12*
